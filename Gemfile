# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-minifier'
end

gem "webrick", "~> 1.9"
gem "jekyll", "~> 4.4.1"  # Add this to explicitly specify Jekyll version